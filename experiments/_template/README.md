# Neues Experiment

Lege hier alles ab, was nur zu diesem Experiment gehoert.

Start:

```bash
uv run experiments/<name>/main.py
```

Wenn du weitere Module brauchst, halte sie in diesem Ordner, damit das Experiment isoliert bleibt.
